spring:
  application:
    name: notification-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5435/notification_db}
    username: ${DB_USER:techlab}
    password: ${DB_PASS:techlab}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:techlab}
    password: ${RABBITMQ_PASS:techlab}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: manual
        prefetch: 50
        retry:
          enabled: true
          initial-interval: 1000ms
          max-attempts: 4
          multiplier: 2.0
          max-interval: 30000ms

server:
  port: ${SERVER_PORT:8084}
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# Simulated notification provider knobs used by the experiment plan
notification:
  simulator:
    latency-ms: ${NOTIFICATION_LATENCY_MS:200}
    failure-rate: ${NOTIFICATION_FAILURE_RATE:0.0}

logging:
  level:
    org.springframework.amqp: INFO
    com.techlab.ecommerce: DEBUG
  pattern:
    level: "%5p [%X{requestId:-}]"
